modules:
  http_2xx:
    prober: http
    timeout: 5s
    http:
      preferred_ip_protocol: ip4
      valid_status_codes: []   # 2xx default
      method: GET
      fail_if_ssl: false
      fail_if_not_ssl: false
