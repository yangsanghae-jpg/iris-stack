# M5 M2-독립 작업 실행 보고서

- **실행 일시:** 2026-06-05 12:43 (M5 `irisM5.local`)
- **전제:** diagnosis-tool 작업 진행 중 — M2 commit 없이 M5 단독 실행 가능 항목 처리
- **선행:** [M5_DB분리_및_추가진단_보고서_2026-06-05.md](./M5_DB분리_및_추가진단_보고서_2026-06-05.md)

---

## Executive Summary

| # | 작업 | Phase | 결과 |
|---|------|-------|------|
| 1 | `storage/` → `~/iris-local/storage` | 0.5.1 | ✅ 완료 |
| 2 | launchd 일일 DB 백업 plist | 0.1 | ✅ 등록 + 수동 테스트 OK |
| 3 | oMLX BGE-M3 다운로드 | 5.4 사전 | 🟡 **미완** — BGE-M3 없음, Qwen3.5-4B 부분 다운로드만 |
| 4 | `.venv` / `__pycache__` 청소 | 0.5 사전 | ✅ 완료 (32MB venv 제거) |
| 5 | `iris-net` / compose 확인 | 0.5.4 사전 | ✅ 정상 |
| 6 | `.gitignore` / 0Dev git | 0.5.7 | ⏸️ **보류** — M2 상의 후 |

**`~/iris-local/` 정본 구조 (iCloud 밖):**

```text
~/iris-local/
├── backups/          # _index.db 일일 백업 (14일 회전)
│   ├── _index.db.20260605_1226
│   └── _index.db.20260605_1243_manual
└── storage/          # K5 telemetry·wiki history
    ├── _index.db (placeholder 0B)
    └── sqlite/wiki_history.db
```

**코드 경로 (symlink 유지, compose 수정 불필요):**

```text
iris-system/storage → ~/iris-local/storage
iris-system/knowledge/_index.db → .nosync/_index.db
```

---

## 🔴 1. storage/ 분리 (Phase 0.5.1)

### 실행

```bash
mkdir -p ~/iris-local/storage
mv ~/Documents/0Dev/iris-system/storage/* ~/iris-local/storage/
rmdir ~/Documents/0Dev/iris-system/storage
ln -sf ~/iris-local/storage ~/Documents/0Dev/iris-system/storage
```

### 검증

| 확인 | 결과 |
|------|------|
| `iris-system/storage` | `lrwxr-xr-x → /Users/iris/iris-local/storage` |
| 실데이터 | `~/iris-local/storage/sqlite/wiki_history.db` 존재 |
| Promtail 마운트 | `docker exec iris-promtail ls /mnt/iris-system-storage/` → **정상** |
| `_index.db` symlink | `SELECT COUNT(*) → 630` **정상** |

K5 telemetry 시작 시 로그가 iCloud 밖 `~/iris-local/storage/`에 쓰이게 됨.

---

## 🔴 2. launchd 일일 백업 (Phase 0.1)

### 등록

```text
파일: ~/Library/LaunchAgents/local.iris.backup-index-db.plist
스케줄: 매일 03:17
동작: cp knowledge/.nosync/_index.db → ~/iris-local/backups/_index.db.YYYYMMDD_HHMM
회전: 14일 초과 파일 자동 삭제
로그: /tmp/iris-backup.log, /tmp/iris-backup.err
```

### 검증

```bash
launchctl list | grep iris
# -  0  local.iris.backup-index-db   ← 등록됨 (exit 0)

# 수동 1회 실행
cp ... → _index.db.20260605_1243_manual  (1.9 MB) ✅
```

**참고:** `launchctl load` 대신 macOS Sequoia 계열에서는 `launchctl bootstrap gui/$(id -u)` 사용함.

---

## 🟡 3. oMLX BGE-M3 (Phase 5.4 사전)

### 현황

| 항목 | 상태 |
|------|------|
| `~/.omlx/models/` | BGE-M3 **없음** |
| 부분 다운로드 | `mlx-community/Qwen3.5-4B-OptiQ-4bit` — **미완** (config/tokenizer만, `model.safetensors` 본체 없음) |
| `/v1/models` | `data: []` |
| HF admin search API | `Admin authentication required` (별도 admin 토큰 필요) |
| `huggingface-cli` | 미설치 |

### embeddings 테스트

실행하지 않음 — 로드된 임베딩 모델 없음.

### 다음 시도 (M5 단독)

1. oMLX UI에서 `bge-m3-mlx` 또는 `bge-m3-mlx-6bit` 검색·다운로드 (hf-mirror 경로는 Qwen에서 동작 확인됨)
2. 또는 `pip install huggingface_hub` 후 `hf download mlx-community/bge-m3-mlx --local-dir ~/.omlx/models/...`
3. 다운로드 후:

```bash
curl -sS -H "Authorization: Bearer <API_KEY>" \
  http://127.0.0.1:8080/v1/embeddings \
  -H "Content-Type: application/json" \
  -d '{"model":"<model-id>","input":"한국어 테스트"}'
```

---

## 🟡 4. iris-system 사전 청소

| 작업 | 결과 |
|------|------|
| `rm -rf iris-system/.venv` | ✅ 제거 (32 MB, python3.14 깨진 venv) |
| `__pycache__` | ✅ 2개 디렉터리 삭제 → 0개 |

Docker(옵션 A) 전환과 일치. M2에서 Dockerfile 수신 후 재작업 불필요.

---

## 🟡 5. iris-stack Docker 네트워크 (Phase 0.5.4 사전)

### `iris-net`

```text
Driver: bridge
Subnet: 172.18.0.0/16
Gateway: 172.18.0.1
```

### 연결 컨테이너 (11개)

```text
iris-l2-gateway, iris-l4-search, iris-memory-admin,
iris-claw, iris-prometheus, iris-grafana, iris-loki,
iris-promtail, iris-cadvisor, iris-node-exporter, iris-blackbox-exporter
```

### `docker compose ps` (iris-stack base)

| 서비스 | 상태 |
|--------|------|
| l2-gateway | Up 13h → `:8011` |
| l4-search | Up 13h → `:8020` |
| memory-admin | Up 13h → `:18020` |
| observability 7종 | Up 13h |

**판정:** `iris-k5-router` / `iris-k5-wiki` 추가 시 `iris-net`에 join만 하면 됨. 네트워크 선행 작업 불필요.

**참고:** `iris-memory`·`iris-claw`는 base compose에 없고 별도 기동이나 observability overlay에 포함된 상태로 `iris-net`에 존재.

---

## 🟢 6. .gitignore / 0Dev git — 보류

마스터 지시대로 **M2와 상의 후** 진행.

- `iris-system` git 없음
- 0Dev 루트 git 초기화는 diagnosis-tool 작업·M2 commit 타이밍과 맞춰야 함
- 권장 선행 항목: `.gitignore`에 `.venv/`, `__pycache__/`, `knowledge/.nosync/`, `~/iris-local` 경로 안내

---

## M5에서 남은 일 (M2 의존 / 후속)

| 우선순위 | 작업 | M2 필요? |
|----------|------|----------|
| 🔴 | iris-system Dockerfile + compose (router 18080, wiki 18081) | compose/Dockerfile은 M2 작성 권장 |
| 🔴 | K5 router 기동 → telemetry → Grafana 패널 | Docker 추가 후 |
| 🟡 | oMLX BGE-M3 완료 다운로드 | M2 불필요 |
| 🟡 | Qwen3.5-4B oMLX 부분 다운로드 정리 (완료 or 삭제) | M2 불필요 |
| 🟢 | 0Dev git + `.gitignore` | M2 상의 후 |
| 🟢 | L6 llm-gateway K5 PoC | M2 코드 후 M5 테스트 |

---

## Phase 진행 체크리스트 (M5 기준)

- [x] Phase 0.1 — launchd 백업 plist
- [x] Phase 0.5.1 — `storage/` iCloud 분리
- [x] Phase 0.5.1 — `_index.db` `.nosync` 분리 (이전 세션)
- [x] Phase 0.5 사전 — 깨진 `.venv` 제거
- [ ] Phase 0.5.4 — iris-system Docker compose 추가 (M2)
- [ ] Phase 5.4 — oMLX BGE-M3
- [ ] Phase 0.5.7 — git / `.gitignore` (M2)

---

## 부록: 실행 명령 요약

```bash
# 1. storage 분리 ✅
mkdir -p ~/iris-local/storage
mv ~/Documents/0Dev/iris-system/storage/* ~/iris-local/storage/
ln -sf ~/iris-local/storage ~/Documents/0Dev/iris-system/storage

# 2. launchd 백업 ✅
# ~/Library/LaunchAgents/local.iris.backup-index-db.plist
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/local.iris.backup-index-db.plist

# 4. 청소 ✅
rm -rf ~/Documents/0Dev/iris-system/.venv
find ~/Documents/0Dev/iris-system -name __pycache__ -type d -exec rm -rf {} +

# 5. docker ✅
docker network inspect iris-net
docker compose -f ~/Documents/0Dev/iris-stack/docker-compose.yml ps
```

---

*생성: M5 M2-독립 작업 큐 실행 · 2026-06-05*
