# 연결성 점검 보고서

> **점검일:** 2026-05-18
> **점검 환경:** macOS 가상 머신 (Parallels) 내 VS Code / Claude Code
> **점검 목적:** 가상 macOS에서 호스트 M5 Mac의 개발 환경과 연결 가능 여부 확인
> **점검 원칙:** 읽기/확인 명령만 실행, 파일 수정 없음

---

## 1) 실행 위치

- **사용자:** iris-vm
- **호스트명:** bogon
- **현재 경로:** /Volumes/0Dev
- **OS:** macOS 26.5 (Darwin 25.5.0, arm64 VMAPPLE)
- **VM IP:** 10.211.55.11 (en0)

## 2) Claude/Node/VS Code 상태

| 항목 | 결과 |
|---|---|
| claude | ✅ `/opt/homebrew/bin/claude` — v2.1.143 |
| node | ✅ `/opt/homebrew/bin/node` — v26.0.0 |
| npm | ✅ `/opt/homebrew/bin/npm` — v11.12.1 |
| code | ✅ `/opt/homebrew/bin/code` — v1.120.0 (arm64) |

## 3) AI Hub 설정 상태

| 항목 | 결과 |
|---|---|
| ANTHROPIC_BASE_URL | `https://ai.semi-tech.com` |
| ANTHROPIC_MODEL | `claude-opus-4.6` |
| ANTHROPIC_AUTH_TOKEN | **설정됨 (YES)** — 값 미출력 |

설정 흔적:
- ✅ `~/.claude/` 존재 (settings.json, projects, sessions 등)
- ✅ `~/.cc-switch/` 존재 (cc-switch.db, settings.json)
- ❌ `~/.config/` 없음
- ✅ `~/Library/Application Support/` 존재

## 4) 호스트 연결성

| 대상 | 결과 |
|---|---|
| 10.211.55.2 ping | ❌ 100% packet loss (ICMP 차단 가능성) |
| SSH 22 | ❌ Connection refused |
| 8000 | ❌ Connection refused |
| 8010 | ❌ Connection refused |
| 18789 | ❌ Connection refused |
| 11434 | ❌ Connection refused |
| 1234 | ❌ Connection refused |

> 모두 "Connection refused" — TCP 레벨로는 호스트가 RST를 응답한다는 뜻이므로 호스트 자체에는 도달하나, **해당 포트들은 모두 LISTEN 상태가 아님**.

## 5) HTTP Health

| URL | 결과 |
|---|---|
| http://10.211.55.2:8000/health | ❌ HTTP 000 (1s 타임아웃/거부) |
| http://10.211.55.2:8010/health | ❌ HTTP 000 |
| http://10.211.55.2:18789/health | ❌ HTTP 000 |
| http://10.211.55.2:11434 | ❌ HTTP 000 |
| http://10.211.55.2:1234/v1/models | ❌ HTTP 000 |

## 6) 공유 폴더 / 프로젝트

| 경로 또는 프로젝트 | 결과 |
|---|---|
| /Volumes/0Dev | ✅ 마운트됨 (Parallels 공유, SMB로 추정) |
| /Volumes/Documents/0Dev | ❌ 없음 |
| ~/Documents/0Dev | ⚠️ 빈 디렉토리만 존재 |
| iris-claw | ✅ /Volumes/0Dev/iris-claw (git, 변경 다수) |
| iris-memory | ✅ /Volumes/0Dev/iris-memory (.git 없음) |
| iris-gateway | ✅ /Volumes/0Dev/iris-gateway (.git 없음) |
| diagnosis-tool | ✅ /Volumes/0Dev/diagnosis-tool (git, 변경 다수) |
| iris-stack | ✅ /Volumes/0Dev/iris-stack (git, 변경 다수) |
| iris-system | ✅ /Volumes/0Dev/iris-system (.git 없음) |

### Git 저장소 변경 요약

- **iris-claw** — `M` openclaw-workspace/AGENTS.md, SOUL.md, TOOLS.md, package.json / `??` .dockerignore 외
- **diagnosis-tool** — `M` .gitignore, Dockerfile, Makefile, build.sh / `D` build_and_tag_v10.sh 외
- **iris-stack** — `M` docker-compose.yml, l2-gateway/app/main.py, memory-admin/app/main.py 외

## 7) 판정

- **호스트 M5와 통신 가능 여부:** 🟡 **부분 가능** — TCP 도달은 되나(Connection refused = 호스트 살아 있음 + 포트 닫힘), 점검 대상 서비스 7개 포트 모두 **현재 LISTEN 아님**. ICMP는 차단됨.
- **공유 폴더 사용 가능 여부:** ✅ **가능** — `/Volumes/0Dev` 공유 마운트 정상 동작, 모든 프로젝트 폴더 접근/Git 작업 가능.
- **Claude Code가 AI Hub 설정을 인식하는지:** ✅ **인식 중** — `ANTHROPIC_BASE_URL=https://ai.semi-tech.com`, 모델 `claude-opus-4.6`, 토큰 설정됨. `~/.cc-switch/` 및 `~/.claude/settings.json` 모두 존재.

### 다음 조치

1. **호스트 M5에서 서비스 실행 상태 확인**
   ```sh
   lsof -iTCP -sTCP:LISTEN -P | grep -E '8000|8010|18789|11434|1234'
   ```
2. **바인딩 주소 확인** — 서비스가 실행 중이라면 `127.0.0.1`이 아닌 `0.0.0.0` 또는 `10.211.55.2`로 바인딩되어야 VM에서 접근 가능
3. **macOS 방화벽 확인** — Parallels 인터페이스(`vnic0`/`vnic1`) 트래픽을 허용하는지 확인
4. **ICMP 차단** — macOS 기본 동작일 수 있으므로 무시 가능. nc TCP 확인이 더 신뢰할 수 있음
5. **모델 설정 검증** — 환경변수 `ANTHROPIC_MODEL`이 `claude-opus-4.6`인데 현재 세션은 `claude-opus-4-7`로 실행 중 — `/model` 또는 `~/.claude/settings.json`에서 어느 쪽이 우선되는지 확인 필요

---

*점검자: Claude Code (Opus 4.7) / 자동 생성 보고서*
